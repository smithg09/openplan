---
name: openplan-archive
description: Open the Openplan plans dashboard in the browser
---

Run the following shell command:

```bash
openplan serve
```

This opens the Openplan dashboard in the browser, showing all saved plans
organized by project. The server runs in the background. No output is
injected into the conversation — this is a browse-only action.
