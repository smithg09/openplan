---
description: Open the Openplan plans dashboard in the browser
allowed-tools: shell(openplan:*)
---

## Openplan Archive

!`openplan serve`

## Your task

This starts the Openplan dashboard server in the background and opens it in
the browser, showing all saved plans organized by project. This is a
browse-only action — there is no output to act on, so just let the user know
the dashboard is open.
