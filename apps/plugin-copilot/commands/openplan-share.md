---
description: Generate a shareable browser link for a plan file so collaborators can view and annotate it without installing anything
allowed-tools: shell(openplan:*)
---

Run the following, substituting `$ARGUMENTS` with the file path the user
provided:

## Openplan Share

!`openplan share $ARGUMENTS`

## Your task

Show the user the generated URL printed above and let them know it has been
copied to their clipboard (if the command confirms so). Collaborators can
open it in any browser to read the plan and add annotations — no account or
installation required.
