name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Landing Page
        run: bun run build:landing

      - name: Build Demo App
        run: VITE_GITHUB_PAGES=true bun run build:ui

      - name: Prepare Pages output
        run: |
          mkdir -p _site/app
          # Landing page at root
          cp -r apps/landing-page/dist/* _site/
          # Demo app under /app/
          cp -r apps/cli/internal/server/ui/dist/* _site/app/
          # Install script
          cp scripts/install.sh _site/install.sh
          # Custom domain
          echo "openplan.smithgajjar.dev" > _site/CNAME

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

